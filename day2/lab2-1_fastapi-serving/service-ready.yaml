# Lab 2-1: Kubernetes Service for Iris API
# ==========================================
#
# 사용법:
#   1. kubeflow-user00를 자신의 네임스페이스로 변경
#   2. kubectl apply -f service.yaml

apiVersion: v1
kind: Service
metadata:
  name: iris-api-svc
  namespace: kubeflow-user00
  labels:
    app: iris-api
spec:
  type: ClusterIP
  selector:
    app: iris-api
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
